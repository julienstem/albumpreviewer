export interface Metadata {
    title: string;
    artist: string|string[]|null;
    album: string|null;
    year: string|null;
    genre: string|string[]|null;
    cover: string|null;
    duration: string;
}